package tests;

import org.testng.annotations.Test;

public class UpdatePetTest extends BaseTest {

    @Test(description = "Test Case 3 - Update a Pet")
    public void updatePet() {
        // TODO: create the pet; assert status code and add its id to createdPetIds for teardown

        // TODO: update the pet's category via PUT; assert status code and the response category name

        // TODO: retrieve the pet again and assert its category name matches the update
    }
}
